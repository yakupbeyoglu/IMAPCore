#include "imap/response.h"

// Nothing beyond the header is needed for the response data types —
// they are purely structural (no methods with non-trivial implementations).
// This file is intentionally minimal.

namespace imap {}  // namespace imap
